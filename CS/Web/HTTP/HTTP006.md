# HTTP
## 2026-04-29 Wed
### 웹 로봇이란?
- 웹 로봇은 사람과의 상호작용 없이 연속된 웹 트랜잭션을 자동으로 수행하는 소프트웨어를 지칭한다.
  - 많은 로봇이 웹 사이트들을 떠돌아다니며 컨텐츠를 조회하거나 링크를 따라가는 방식으로 발견한 데이터를 처리한다.
  - 이러한 로봇들은 모두 자동으로 웹 사이트들을 탐색하며, 방식에 따라 크롤러나 스파이더 또는 웜 및 봇 등의 용어로 지칭된다.
- 웹 로봇은 예를 들어 다음과 같은 작업들을 수행할 수 있다.
  1. 월드 와이드 웹의 규모와 진화와 관련한 통계 정보를 수집
  2. 검색 데이터베이스를 구성하기 위해 발견한 모든 문서를 수집하는 검색엔진 봇

## 2026-04-30 Thu
### 크롤러와 스파이더
- 웹 크롤러는 임의의 페이지를 하나 조회한 후, 그 페이지가 참조하는 모든 페이지를 다시 조회하는 식으로 재귀적으로 동작한다.
  - 이렇듯 링크에 따라 웹을 순회하며 재귀적으로 동작하는 모습이 마치 기어가는 것 같아 크롤러 또는 스파이더라는 용어로 지칭한다.
  - 반면, 크롤러가 수집해야할 대상은 매우 많기에 이들은 특성상 복잡한 로직을 가질 수 밖에 없다.
- 검색엔진은  웹을 순회하며 마주치는 모든 문서를 수집하기 위해 크롤러를 사용하며, 이렇게 수집된 문서는 후처리되어 검색 가능한 데이터베이스에 등록된다.

## 2026-05-01 Fri
### 크롤링과 루트 집합
- 크롤러를 동작시키기 위해서는 우선 시작 지점인 루트 집합을 설정해야 하지만, 최종적으로 세상의 모든 문서에 이어질 수 있는 하나의 문서란 존재하지 않는다.
- 때문에 여러 개의 루트 집합으로부터 크롤링을 시작하는 경우 많으며, 일반적으로 좋은 루트 집합은 다음을 포함한다.
  1. 야후와 같은 크고 인기 있는 웹 사이트
  2. 새로 생성된 페이지 목록
  3. 자주 링크되지 않는, 잘 알려지지 않은 페이지 목록
- 대부분의 대규모 크롤러는 루트 집합에 새 페이지나 잘 알려지지 않은 페이지를 추가하는 기능을 제공하며, 이들 루트 집합은 시간에 따라 성장하는 경향을 보인다.

## 2026-05-012 Sat
### 크롤링과 순환 회피
- 크롤러는 동작 과정에서 꾸준히 HTML 문서를 수집하고, 해당 문서 내에 포함된 URL 링크를 파싱하여 크롤링 대상 페이지 목록에 추가한다.
  - 이 때, 크롤러는 HTML 파싱을 통해 링크를 추출하고 상대 링크를 절대 링크로 변환한다.
- 크롤러의 동작 과정에서 단연 중요한 것은 루프나 순환에 빠지지 않는 것으로, 이는 크게 다음과 같은 이유로 인해 해롭다고 볼 수 있다.
  1. 순환: 크롤러가 루프에 빠져 정상적으로 동작하지 않을 수 있다.
  2. 부하: 크롤러가 임의의 페이지를 계속해서 요청할 경우, 해당 페이지를 제공하는 서버의 부담이 된다.
  3. 중복: 루프 자체가 큰 문제가 되지 않더라도, 크롤러는 수많은 무의미한 중복 컨텐츠를 반환할 수 있다.
- 때문에 크롤러는 순환을 회피해야 하며, 수십 억개 이상의 URL에 대한 방문 기록을 유지하기 위해 다음과 같은 기법을 고려할 수 있다.
  1. 트리 및 해시 테이블: 방문한 URL을 빠르게 조회할 수 있도록 하는 자료 구조에 해당한다.
  2. 느슨한 존재 비트맵: 방문한 URL은 해시 함수에 의해 고정된 크기의 숫자로 변환되고, 배열 내에 대응하는 존재 비트를 두어 검증한다.
  3. 체크포인트: 크롤러가 예상치 못하게 중단된 경우에 대비하여 방문한 URL 목록을 디스크에 저장한다.
  4. 파티셔닝: 웹이 성장함에 따라 하나의 크롤러가 크롤링을 완수하는 것은 불가능해졌기에, 여러 크롤러로 구성된 `farm`을 통해 크롤링을 수행한다.