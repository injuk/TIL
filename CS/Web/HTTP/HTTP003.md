# HTTP
## 2026-01-16 Fri
### HTTP와 TCP/IP의 관계
```
> HTTP 커넥션은 대부분의 경우 TCP 커넥션에 불과하다.
```
- 일반적인 HTTP 역시 TCP/IP를 기반으로 하며, 임의의 HTTP 요청은 다음과 같이 처리된다.
  1. 브라우저는 URL로부터 호스트 정보를 추출하여 적절한 IP 주소를 찾는다.
  2. 브라우저는 URL로부터 포트 정보를 추출하여 서버측 애플리케이션으로 향하는 TCP 커넥션을 생성한다.
  3. 브라우저는 2.의 커넥션을 통해 HTTP GET 요청 메시지를 전송한다.
  4. 서버는 요청에 대한 처리 결과를 반환한다.
  5. 브라우저는 서버의 응답을 읽어들인 후, 커넥션을 끊는다.

## 2026-01-17 Sat
### TCP란?
- TCP/IP를 사용하는 클라이언트는원하는 때에 서버로 TCP/IP 커넥션을 맺을 수 있으며, 이 경우 메시지는 손상되거나 순서가 바뀌지 않고 전달된다.
  - 때문에 HTTP로 메시지를 전송하는 경우, 현재 연결된 TCP 커넥션을 통해 메시지에 포함된 데이터가 순차적으로 전달된다.
- TCP는 세그먼트라는 단위로 데이터 스트림을 나누고, 이렇게 나뉘어진 세그먼트를 다시 IP 패킷에 담아 전달한다.
  - 이러한 과정은 모두 TCP/IP 계층을 담당하는 애플리케이션에 의해 처리되므로, HTTP 개발자는 이를 신경쓸 필요가 없다.
  - IP 패킷은 IP 패킷 헤더와 TCP 세그먼트 헤더 및 TCP 데이터 조각으로 구성된다.
  - 반면, TCP 세그먼트 헤더는 TCP 포트 번호와 제어 플래그 및 데이터 무결성을 검증하기 위한 숫자 값을 포함한다.

## 2026-01-18 Sun
### TCP 커넥션 구성 요소
- 컴퓨터는 기본적으로 여러 개의 TCP 커넥션을 처리하며, TCP는 포트 번호를 통해 여러 개의 커넥션을 유지한다.
- 이 때, **각각의 TCP 커넥션은 다음과 같은 네 가지 구성 요소의 조합으로 유일하게 식별**된다.
  1. 발신지 IP 주소
  2. 발신지 포트 번호
  3. 수신지 IP 주소
  4. 수신지 포트 번호
- 때문에 임의의 서버는 동일한 포트 번호에서 여러 개의 TCP 커넥션을 구분하여 관리할 수 있다.
  - 반면, **현대적인 서버는 대부분의 경우 하나의 리스닝 포켓을 열어두고 새로운 연결 요청에 대해 전용 소켓을 생성하여 연결을 수립**하는 경우가 많다.

## 2026-01-19 Mon
### TCP 소켓 프로그래밍이란?
- 운영체제는 TCP 커넥션 생성과 관련된 여러 기능을 제공하며, 이를 위한 TCP 프로그래밍 인터페이스를 구현하는 소켓 API는 TCP와 IP의 세부 구현을 숨긴다.
  - **소켓 API는 유닉스 운영체제용으로 개발되었으나, 현대에 이르러서는 다양한 구현체들로 인해 대부분의 운영체제와 프로그래밍 언어에서 사용**할 수 있다.
- 소켓 API를 사용하면 TCP 엔드포인트가 되는 데이터 구조를 생성하고, 원격 서버의 TCP 엔드포인트에 이를 연결할 수 있다.
  - 이 때, 연결된 후에는 이를 통해 데이터 스트림을 읽고 쓸 수 있다.
  - 반면, **이러한 API는 기본적인 핸드셰이크와 TCP 데이터 스트림의 분할 및 재조립과 관련된 세부 사항을 개발자가 신경쓰지 않도록 숨기는 역할**을 맡는다.

## 2026-01-20 Tue
### HTTP와 TCP 네트워크 지연
- HTTP는 TCP 위에서 동작하는 계층이므로, 트랜잭션의 성능 역시 TCP 성능에 직접적인 영향을 받는다.
- HTTP 통신 시간은 DNS 동작 이후에 커넥션 연결과 요청, 요청에 대한 처리와 결과를 응답하고 종료하는 과정 전체를 포함한다.
  - 이 때, 트랜잭션 처리 시간을 제외한 다른 모든 과정을 위해 소요되는 시간에 비해 트랜잭션 처리 시간은 적은 시간을 요구한다.
- 이렇듯 **HTTP 트랜잭션을 지연시키는 직접적인 원인은 TCP 네트워크의 지연**으로, 크게 다음과 같이 정리할 수 있다.
  1. 임의의 호스트에 대한 첫 요청시 발생하는 DNS 처리 시간
  2. 서버로의 TCP 커넥션 수립 요청과 이에 대한 서버의 응답 대기 시간
  3. 커넥션이 수립된 후 이를 통해 HTTP 요청을 전송하고, 서버에 의해 처리되는 시간
  4. 서버에 의해 처리된 결과가 응답되기까지의 시간
- 또한, TCP 네트워크 지연은 하드웨어의 성능과 네트워크 속도 및 요청과 응답의 크기에도 영향을 받을 수 있다.
  - 심지어는 클라이언트와 서버 간의 물리적인 거리나, TCP 자체의 기술적 복잡성 역시 주요한 원인이 될 수 있다.

## 2026-01-21 Wed
### 일반적인 TCP 관련 지연들
- 상술한 요인 외에도, HTTP 기반 개발자에게 영향을 줄 수 있을만한 성능 상의 문제는 다음과 같은 지연들로부터 발생할 수 있다.
  1. TCP 커넥션 핸드셰이크 설정
  2. 혼잡 제어를 위한 TCP의 느린 시작
  3. 네이글 알고리즘
  4. TCP piggyback ACK를 위한 ACK 지연
  5. TIME_WAIT 지연과 포트의 고갈
- 일반적인 기준보다좋은 성능을 목표로 하는 HTTP 애플리케이션을 개발하기 위해서는 이러한 요소를 모두 신경쓸 필요가 있다.

## 2026-01-22 Thu
### TCP 커넥션 핸드셰이크 지연
- 일반적인 3-Way TCP 커넥션 핸드셰이크는 다음과 같이 진행된다.
  1. 클라이언트는 새로운 TCP 커넥션을 생성하기 위해 `SYN` 플래그를 포함하는 작은 패킷을 전송한다.
  2. 서버는 해당 TCP 패킷을 분석하고, 커넥션 요청을 받아들이기 위해 `SYN`과 `ACK`가 포함된 패킷을 전송한다.
  3. 클라이언트는 커넥션 수립 완료를 알리기 위해 `ACK`를 보내며, 이 때 데이터를 함께 전송할 수 있다.
- 그러나 **어떤 때라도 새로운 TCP 커넥션을 여는 과정에서는 위와 같은 연속적인 IP 패킷 교환이 전제**된다.
  - 바꿔말해, 아무리 작은 데이터를 전송할 때라도 TCP 커넥션을 사용하는 이상 패킷 교환은 필수적이다.
  - 즉, **데이터의 크기가 충분히 작다면 데이터 전송보다 패킷 교환에 더 많은 비용을 소모**해야할 수 있다.
- 반면, HTTP 계층의 관점에서 TCP 계층은 추상화되므로 상술한 연속적 패킷 교환 과정은 HTTP 계층에서 알 수 없다.
  - 단지 **HTTP 계층에서 체감할 수 있는 것은 암시적으로 진행되는 TCP 패킷 교환으로 인한 지연** 뿐이다.
- 상술한 **TCP 구성 과정에서 기인하는 지연을 제거하기 위해서는 이미 존재하는 커넥션을 재활용하는 것을 고려**해볼 수 있다.

## 2026-01-23 Fri
### ACK 응답 지연
- **인터넷이라는 환경 자체가 무결한 패킷 전송을 보장하지 않는다는 점에서 TCP는 무결성 보장을 위해 자체적인 확인 체계**를 갖는다.
  - 단적인 예로, 라우터는 과부하 상황에서 패킷을 마음껏 파기할 수 있다.
- 다시 말해, 각각의 TCP 세그먼트는 순번과 무결성 체크섬을 가지며 수신자는 이를 확인하여 수신을 확인하기 위한 ACK를 전송한다.
  - 송신자 입장에서, 임의의 데이터에 대해 일정 시간 안에 ACK를 수신하지 못한 경우 해당 데이터를 재전송한다.
- 이러한 **ACK는 크기가 작기 때문에 TCP는 필요한 데이터에 ACK를 함께 전송할 수 있으며, 이를 piggyback이라는 용어로 지칭**한다.
  - 이는 즉 데이터 패킷과 ACK를 하나로 묶어 네트워크를 효율적으로 사용하기 위함이다.
- 반면, **많은 TCP 구현체는 piggyback의 빈도를 높이기 위해 ACK 응답 지연 알고리즘을 구현**한다.
  - 예를 들어, 전송해야 할 ACK를 별도의 버퍼에 일정 시간 저장해둔 후 그 시간 내에 송출 데이터가 발생하면 piggyback으로 반환한다.
  - 반면, 일정 시간 내에 송출 데이터가 발생하지 않는다면 ACK는 별도의 패킷으로 전송된다.
- 그러나 **HTTP 트랜잭션은 요청과 응답으로 구성되므로, piggyback이 발생할 가능성이 낮아 ACK 응답 지연으로 인한 지연이 발생**하기 쉽다.
- 물론 ACK 지연 관련 기능을 수정하거나 비활성화하는 것은 가능하지만, 이러한 설정으로 인해 부작용이 발생하지 않는다는 명확한 근거 하에 진행해야 한다.

## 2026-01-24 Sat
### TCP의 느린 시작
- TCP의 데이터 전송 속도는 TCP 커넥션이 생성되고 얼마나 지났는지에 따라 달라질 수 있으며, 이렇듯 **TCP 커넥션은 시간에 따라 자체적으로 최적화**된다.
  - 예를 들어, 최초에는 최대 속도를 제한하되 점진적으로 속도 제한을 높여나간다.
  - 이러한 TCP의 동작 방식을 **느린 시작이라는 용어로 지칭하며, 이는 인터넷 상에서 발생할 수 있는 혼잡을 방지하는 데에 의의**를 갖는다.
- TCP의 느린 시작은 TCP가 한 번에 전송할 수 있는 패킷 수를 제헌하며, ACK를 받아갈수록 전송 가능한 패킷의 양이 늘어난다.
  - 이는 `congestion window`와 관련이 있으며, 이로 인해 새로운 커넥션은 보다 오래 사용된 커넥션보다 느리게 동작한다.
- 즉, **HTTP 통신의 경우 일반적으로 이미 생성된 TCP 커넥션을 재사용하는 것이 성능 상 유리**할 수 있다.

## 2026-01-25 Sun
### 네이글 알고리즘과 TCP_NODELAY
- 각 TCP 세그먼트는 여러 플래그와 헤더가 포함되므로, 크기가 작은 데이터를 포함한 세그먼트를 많이 전송할 경우 네트워크 성능은 크게 저하된다.
- 이 때, **네이글 알고리즘은 네트워크 효율성을 위해 패킷을 전송하기 전에 많은 TCP 데이터를 하나로 합치는 방식으로 동작**한다.
  - 즉, 세그먼트가 사실상 최대 크기가 되지 않는다면 전송을 하지 않는다.
  - 반면, 다른 모든 패킷이 ACK를 수신한 경우라면 최대 크기보다 작은 패킷까지도 전송 가능하다.
- 다시 말해, 다른 패킷들이 아직 전송 중인 상황에서는 새로운 패킷은 버퍼에 저장된다.
  - 전송 중이던 패킷에 대한 ACK를 수신 받았거나, 버퍼의 데이터가 충분히 쌓인 경우에만 전송된다.
  - 때문에 네이글 알고리즘은 크기가 작은 HTTP 메시지는 패킷의 크기가 충분하지 않아 오랜 시간 지연될 가능성을 갖는다.
- ACK에 의존적인 네이글 알고리즘은 ACK 지연과 함께 동작할 때 큰 성능 저하를 보이게 되며, 이를 제어하기 위해 `TCP_NODELAY` 파라미터를 설정할 수 있다.
  - 해당 플래그는 네이글 알고리즘을 비활성화하며, 이 경우 너무 작은 데이터가 많이 생기지 않도록 데이터를 적절히 모아줄 필요가 있다.

## 2026-01-26 Mon
### TIME_WAIT 누적과 포트의 고갈
- TCP 커넥션 엔드포인트에서 커넥션을 종료한 경우, 엔드포인트는 커넥션의 IP 주소와 포트 번호를 메모리의 `control block`에 임시 저장한다.
  - 이는 **동일한 주소와 포트 번호를 사용하는 새로운 TCP 커넥션이 일정 시간 생성되지 않도록 하기 위함**이다.
- 이를 통해 **이전 커넥션과 관련된 패킷이 새로운 커넥션에 삽입되는 것을 방지**할 수 있다.
- 일반적인 경우 커넥션 종료 지연이 영향을 주지는 않으나, 성능 테스트와 같은 특수한 상황에서는 문제가 될 수 있다.
  - 이는 성능 측정 대상 서버에 대해 접근 가능한 IP 주소의 개수가 제한되는 경우가 많기 때문이다.
- 클라이언트가 서버에 접속할 때마다 유일한 커넥션을 생성하는 경우, 소켓을 구분하기 위한 구성 요소 중 발신지 포트만을 변경할 수 있다.
  - 그러나 사용 가능한 발신지 포트는 제한적이며, `TIME_WAIT` 상태의 커넥션은 재사용될 수 없으므로 결과적으로 커넥션이 제한된다.

## 2026-01-27 Tue
### Connection 헤더란?
```
> Connection 헤더는 HTTP 커넥션 관리에 있어 매우 중요하지만 잘못 이해되는 헤더이기도 하다.
```
- HTTP는 클라이언트와 대상 서버 사이에 프록시 등 여러 중개자가 놓일 수 있으므로, 인접한 HTTP 애플리케이션 사이의 커넥션에만 옵션을 적용해야할 수 있다.
- `Connection` 헤더는 커넥션 토큰을 쉼표로 구분하며, 이는 다른 커넥션에 전달되지 않는다.
- 이 때, `Connection` 헤더는 다음과 같은 토큰을 명시할 수 있다.
  1. HTTP 헤더 이름: 해당 커넥션에만 해당되는 헤더를 명시한다.
  2. 임시 토큰 값: 해당 커넥션에 대한 비표준 옵션을 명시한다.
  3. `close`: 작업 완료 후 커넥션이 종료되어야 함을 의미한다.
- 특히 **HTTP 헤더 이름의 경우, `Connection`에 명시된 헤더는 메시지가 다른 홉으로 전달되는 시점에 삭제**되어야 한다.
- 예를 들어, `Connection: meter, close, bill-my-credit-cards` 헤더는 다음과 같은 의미를 갖는 것으로 해석할 수 있다.
  1. `Meter` 헤더를 다른 커넥션으로 전달하지 않는다.
  2. `bill-my-credit-card`는 비표준 옵션으로 적용해야 한다.
  3. `close`에 의해 현재 트랜잭션이 종료되는대로 커넥션도 종료되어야 한다.

## 2026-01-28 Wed
### 순차 트랜잭션 처리에 의한 지연 극복하기
- 커넥션을 적절히 관리하지 않는 경우, TCP의 성능은 크게 저하될 수 있다.
- 예를 들어, 여러 컨텐츠를 포함하는 HTML 페이지를 요청할 경우 컨텐츠 별 트랜잭션 개수 + HTML 요청 트랜잭션 개수만큼 지연이 발생한다.
  - 또한, 이러한 지연은 상술한 느린 시작에 의해 예상한 것보다도 더더욱 느려질 수 있다.
  - 이렇듯 **순차 처리로 인해 발생한 지연은 물리적인 지연 뿐만 아니라, 로딩 과정에서 텅 빈 웹페이지를 보는 데에서 오는 심리적인 지연도 발생**하기 쉽다.
- 이러한 문제를 해결하기 위해 HTTP 커넥션을 향상시킬 수 있는, 다음과 같은 여러 기술이 제공된다.
  1. 병렬 커넥션: 여러 TCP 커넥션을 활용한 HTTP 동시 요청
  2. 지속 커넥션: 커넥션을 맺고 끊는 데에서 오는 지연을 제거하기 위한 TCP 커넥션의 재활용
  3. 파이프라인 커넥션: 공유 TCP 커넥션을 활용하는 병렬 HTTP 요청
  4. 다중 커넥션: 요청과 응답들 각각에 대한 중재를 적용

## 2026-01-29 Thu
### 병렬 커넥션이란?
- HTTP는 클라이언트가 여러 개의 커넥션을 맺는 것으로 다수의 HTTP 트랜잭션을 병렬로 처리할 수 있도록 지원한다.
  - 이 때, 웹페이지 내부의 컴포넌트들은 각자 출처가 다를 수 있으므로 병렬 커넥션은 다수의 서버와 맺어질 수 있어야 한다.
- 일반적으로 **병렬 커넥션은 각 커넥션의 지연 시간을 겹치게 하는 것으로 전체 지연을 줄일 수 있기에 더 빠르다**.
  - 그러나 항상 빠른 것은 아니며, 예를 들어 제한된 네트워크 대역폭 내에서는 오히려 느릴 수 있다.
  - 또한, 너무 많은 커넥션은 많은 메모리를 소모하므로 서버 측에서 단일 사용자에 대해 무제한에 가까운 커넥션을 허용하지는 않는다.
  - 예를 들어, 브라우저의 경우 병렬 커넥션을 사용하지만 적은 수의 병렬 커넥션만을 허용한다.
- 반면, 병렬 커넥션이 항상 빠르지는 않지만 여러 객체를 동시에 내려받으므로 사용자로 하여금 더 빠르게 느껴질 수 있도록 할 수 있다.
  - 이는 실제로 전체 페이지의 다운로드 시간이 늘어나더라도, 동시다발적으로 여러 작업이 수행되는 것을 확인하는 데에서 오는 심리적인 착각에 가깝다.

## 2026-01-30 Fri
### 지속 커넥션이란?
- 일반적인 HTTP 클라이언트는 동일한 서버에 대해 여러 개의 커넥션을 맺으며, 이를 사이트 지역성이라고 지칭한다.
  - 단적으로, 동일한 웹 페이지에 배치된 리소스들은 대부분 동일한 출처에 위치한다.
  - 때문에 웹 페이지의 모든 리소스를 조회하기 위해서는 동일한 서버에 대한 요청이 여러번 발생한다.
- 이러한 특성에서 **HTTP/1.1은 임의의 요청에 대한 처리가 완료된 후에도 TCP 커넥션을 유지하는 것으로 재사용성을 높이는 특징**을 갖는다.
  - 이렇듯 처리 완료 후에도 연결된 상태로 유지되는 TCP 커넥션은 지속 커넥션이라는 용어로 지칭한다.
- 비지속 커넥션은 처리 완료 후 커넥션을 끊지만, 지속 커넥션은 명시적인 종료 전에는 유지된다.
- **지속 커넥션은 커넥션을 맺기 위한 준비 작업에 필요한 시간과, 느린 시작으로 인한 지연을 회피할 수 있어 유용**하다.
- 반면, 지속 커넥션은 HTTP 버전에 따라 다음과 같은 종류로 구분될 수 있다.
  1. HTTP/1.0: `keep-alive` 커넥션
  2. HTTP/1.1: 지속 커넥션

## 2026-01-31 Sat
### 병렬 커넥션과 지속 커넥션의 비교
- 병렬 커넥션은 매번 새로운 커넥션을 생성하고, 느린 시작의 문제를 피할 수 없다는 단점을 갖는다.
  - 반면, 지속 커넥션은 TCP 커넥션 자체의 지연을 회피하고 최적화된 커넥션을 유지할 수 있다는 장점을 갖는다.
- 지속 커넥션은 잘못 관리될 경우 계속 연결된 상태로 남는 수많은 커넥션에 의해 클라이언트와 서버의 불필요한 리소스 사용을 유발할 수 있다.
- 이러한 특징에서, **두 방식은 혼합하여 사용할 때 가장 효과적이며 수많은 애플리케이션은 적은 수의 병렬 커넥션을 맺은 후 이를 유지하는 방식을 취**한다.

## 2026-02-01 Sun
### HTTP/1.0+와 keep-alive 커넥션
- `keep-alive` 커넥션은 커넥션을 맺고 끊는 데에 필요한 작업이 없어 시간이 단축되며, 느린 시작을 회피할 수 있다는 점에서 지속 커넥션의 장점을 갖는다.
- `keep-alive` 커넥션을 위해서는 클라이언트와 서버 모두가 헤더에 `Connection: Keep-Alive`를 명시할 필요가 있다.
  - 요청을 받은 서버는 클라이언트가 동일한 커넥션을 유지하기를 기대한다는 사실을 알 수 있다.
  - 클라이언트는 서버로부터의 응답에 해당 헤더가 포함된 경우 커넥션이 유지된 다는 것을 알 수 있다.
  - 반면, 누락된 경우 서버가 지속 커넥션을 지원하지 않아 커넥션이 끊길 것으로 추정하게 된다.
  - 이렇듯 헤더에 명시된 내용은 어디까지나 요청일 뿐이므로, 서버가 이를 반드시 따를 의무는 존재하지 않는다는 점에 주의해야 한다.
- `keep-alive` 커넥션과 관련된 옵션은 `Keep-Alive: max=5, timeout=120`과 같이 쉼표로 구분 된 키-값 쌍의 모음으로 명시할 수 있다.
  - 그러나 해당 헤더는 `Connection: Keep-Alive` 헤더가 명시된 경우에만 사용이 가능하다.

## 2026-02-02 Mon
### keep-alive 커넥션의 규칙
- `keep-alive` 커넥션은 HTTP/1.0에서 기본으로 사용되지 않으며, 반드시 `Connection` 헤더를 명시해야 한다.
- 클라이언트와 서버는 `Connection: Keep-Alive` 헤더가 명시된 것을 통해 커넥션 유지 여부를 결정하거나 추정할 수 있다.
  - 즉, 서버는 반드시 이를 따를 필요 없으며 클라이언트 역시 서버가 요청에 응하지 않을 수 있다는 점을 이해해야 한다.
- `keep-alive` 커넥션은 `Content-Length` 헤더가 정확히 명시되어야 하며, 이를 통해 트랜잭션의 시작과 끝을 명확히 인식하는 것이 가능하다.
- 클라이언트는 모든 응답을 수신하기 전에 커넥션이 끊어진 경우, 별다른 이유가 없다는 가정 하에 요청을 다시 보낼 수 있어야 한다.

## 2026-02-03 Tue
### keep-alive와 멍청한 프록시
- 일반적인 프록시는 `keep-alive` 커넥션과 `Connection` 헤더를 이해하지 못하며, 이로 인해 해당 헤더를 삭제하지 않고 그대로 전달한다.
- 때문에 `keep-alive` 커넥션을 지원하는 클라이언트와 서버는 각각 서로와 지속 커넥션이 생성되었다고 오판할 수 있다.
  - 실제로는 중재자인 프록시가 클라이언트 및 서버 각각에 대해 지속 커넥션을 유지한다.
- 클라이언트는 프록시와의 커넥션에 요청을 지속적으로 전달하지만, 프록시는 동일한 커넥션에서 오는 요청을 예상할 수 없기에 이를 무시한다.
- 반면 프록시는 `keep-alive` 커넥션을 이해하지 못해 서버의 연결 종료를 기대하지만, 서버는 `keep-alive` 커넥션 유지를 위해 연결을 종료하지 않는다.
  - 결과적으로 브라우저와 같은 클라이언트는 타임아웃으로 인해 커넥션이 끊길 때까지 대기하게 된다.
- 이러한 문제를 피하기 위해서는 **프록시는 `Connection`이나 `Keep-Alive`와 같은 홉 별 헤더를 절대 전달하지 않아야** 한다.
  - 프록시는 이러한 홉 별 헤더를 이해하지 못하는 경우, 단순히 확장 헤더로 취급하여 헤더를 전달한다.

## 2026-02-04 Wed
### Proxy-Connection과 영리한 프록시
- 멍청한 프록시 문제를 해결하기 위해, `Proxy-Connection`과 같은 비표준 헤더가 제안되었으며 이를 이해하는 프록시는 영리한 프록시로 지칭할 수 있다.
- 멍청한 프록시가 해당 헤더를 전달 받은 경우 이를 확장 헤더로 이해하여 서버에 전달하지만, 서버는 이를 무시하므로 문제가 되지 않는다.
  - 반면, 영리한 프록시는 이를 `Connection` 헤더로 변환하여 전달하기에 멍청한 프록시 문제를 해결할 수 있다.
- 그러나 이러한 방식은 영리한 프록시 하나만 존재하는 경우에 유효하며, 멍청한 프록시가 경로에 포함되는 경우를 해결할 수 없다.
- 또한, 대부분의 경우 **리버스 프록시는 숨겨지므로 브라우저가 이들을 인식하여 `Proxy-Connection` 헤더를 요청에 포함하는 것 역시 불가능**하다.
  - 이렇듯 **보이지 않는 웹 애플리케이션들이 지속 커넥션 기능을 명확히 구현하는 것은 매우 중요**하다.

## 2026-02-05 Thu
### HTTP/1.1과 지속 커넥션
```
> HTTP/1.1은 `keep-alive` 커넥션을 지원하지 않는 대신, 더 개선된 버전의 지속 커넥션을 지원한다.
```
- **HTTP/1.1이 제공하는 지속 커넥션은 기본적으로 활성화되어 있으므로, 별도의 설정이 없는 경우 모든 커넥션은 지속 커넥션으로 취급**된다.
- 때문에 HTTP/1.1 애플리케이션이 트랜잭션 종료 후 커넥션을 끊기 위해서는 `Connection: close` 헤더를 명시해야 한다.
  - 바꿔 말해, **HTTP/1.1 클라이언트는 응답에 `Connection: close`가 포함되지 않는 경우 커넥션을 계속해서 유지한다는 것으로 추정**한다.
- 물론 현실에서는 클라이언트와 서버는 언제든지 커넥션을 종료할 수 있으므로, 해당 헤더가 포함되지 않은 것이 영영 커넥션을 유지한다는 것을 의미하지는 않는다.

## 2026-02-06 Fri
### 지속 커넥션의 규칙
- 클라이언트가 요청에 `Connection: close`를 명시한 경우, 클라이언트는 해당 커넥션으로 추가적인 요청을 전송할 수 없다.
  - 바꿔 말해, 더 이상 요청하지 않을 것이라면 해당 헤더를 명시해야 한다.
- **커넥션을 지속하기 위해서는 커넥션 상 주고 받는 메시지 모두에 `Content-Length` 헤더를 정확히 명시**해야 한다.
- HTTP/1.1 프록시는 지속 커넥션을 위해 서버와 클라이언트 각각에 대해 별도의 커넥션을 맺고 유지할 필요가 있다.
- **HTTP/1.1 애플리케이션은 `Connection` 헤더에 명시된 값과는 관계 없이 언제든 커넥션을 종료할 수 있음을 기억**할 필요가 있다.
  - 반면, HTTP/1.1 애플리케이션은 끊어진 커넥션을 복구할 수도 있어야 한다.
- 임의의 클라이언트는 서버의 과부하를 방지하기 위해 필요 최소한의 커넥션만을 유지해야 한다.

## 2026-02-07 Sat
### 파이프라인 커넥션이란?
- HTTP/1.1은 지속 커넥션을 활용하여 요청을 파이프라이닝할 수 있으며, 이를 통해 대기 시간이 긴 네트워크 상 왕복으로 인한 시간을 줄여 성능을 높일 수 있다.
- 또한, HTTP/1.1 애플리케이션은 파이프라인 커넥션을 사용하기 위해 다음과 같은 규칙을 준수해야 한다.
  1. HTTP 클라이언트는 지속 커넥션이라는 확신이 있기 전에는 요청을 파이프라이닝하지 않아야 한다.
  2. HTTP 메시지는 순서 개념이 없으므로, 응답은 요청 순서에 맞게 전송되어야 한다.
  3. HTTP 클라이언트는 커넥션이 언제 끊기더라도 남은 요청을 다시 보낼 준비가 되어 있어야 한다.
  4. HTTP 클라이언트는 POST와 같이 멱등하지 않은 메소드를 파이프라인에 요청하지 않아야 한다.

## 2026-02-15 Sun
### 커넥션 끊기
- 어떠한 HTTP 애플리케이션이든 간에 모두 원하는 때에 TCP 전송 커넥션을 끊을 수 있다.
  - 일반적인 커넥션은 메시지를 모두 전송한 후에 끊지만, 에러가 있는 경우 엉뚱한 곳에서 커넥션이 종료될 수 있다.
- 각 HTTP 응답은 본문의 정확한 길이를 의미하는 `Content-Length`를 명시해야 하며, 클라이언트나 프록시는 커넥션 종료용 HTTP 응답을 받은 후에도 본문 길이를 검증해야 한다.
  - 예를 들어 본문 길이가 실제와 맞지 않거나, 헤더가 누락된 경우 이를 서버에 질의해야 한다.
- **커넥션은 에러가 없는 상황에서도 언제든지 끊을 수 있으므로, 모든 HTTP 애플리케이션은 예기치 못한 커넥션 종료에 대비할 수 있어야 한다**.
  - 예를 들어, 클라이언트가 트랜잭션 도중에 커넥션이 끊긴 경우 요청이 안전하다면 가능한 한 요청을 재시도해야 한다.
  - 반면, 커넥션이 끊긴 경우 클라이언트는 서버에서 어느 정도로 처리했는지 알 수 없으므로 부작용에 주의해야 한다.
  - 예를 들어, POST와 같이 멱등하지 않은 요청은 재시도 가능하더라도 재전송하지 않아야 한다.

## 2026-02-16 Mon
### 커넥션을 끊는 여러 방식
- TCP 커넥션은 양방향이므로, 양 종단에는 데이터를 읽고 쓰기 위한 입력 및 출력 큐가 배치된다.
  - 예를 들어, 한 쪽의 출력 큐에 쌓인 데이터는 다른 종단의 입력 큐에 전송된다.
- 애플리케이션은 TCP 입력 채널과 출력 채널 중 하나를 끊거나, 둘 다 끊을 수 있으며 이는 각각 절반 끊기와 전체 끊기라는 용어로 지칭한다.
- **단순한 HTTP 애플리케이션은 전체 끊기만을 지원하지만, 되도록이면 예상치 못한 에러 발생에 대비하기 위한 절반 끊기 사용이 권장**된다.
- 일반적인 경우 커넥션의 출력 채널을 끊는 것은 안전하며, 이 경우 반대편 기기는 모든 데이터를 버퍼로부터 조회한 후 커넥션 종료를 인지하게 된다.
  - 반면, **클라이언트에서 더 이상 데이터를 보내지 않을 것을 확신할 수 없는 상황에서 입력 채널을 끊는 것은 위험**하다.
- 이미 끊긴 커넥션에 데이터를 전송할 경우, 서버의 운영 체제는 `connection reset by peer` 메시지로 응답하게 된다.
  - 대부분의 운영 체제는 이를 심각한 에러로 취급하므로, 아직 읽히지 않은 데이터를 모두 삭제하는 방식으로 동작한다.
  - 즉, 응답이 입력 버퍼에 도달했으나 애플리케이션이 이를 아직 읽지 못한 경우에는 입력 버퍼의 데이터가 모두 삭제될 수 있다.

## 2026-02-17 Tue
### 우아하게 커넥션 끊기
- 일반적으로, **우아한 커넥션 끊기는 애플리케이션이 자신의 출력 채널을 먼저 끊은 후 반대 편 종단에 위치한 기기가 출력 채널을 끊을 때까지 대기하는 것을 의미**한다.
  - 이 경우, 커넥션은 상술한 리셋 상황에 처하는 위험 없이 안전하게 종료된다.
- 그러나 **상대 편 기기의 애플리케이션이 절반 끊기는 구현한다는 보장은 없으므로, 연결을 우아하게 종료하려는 애플리케이션은 별도의 구현이 필요**하다.
  - 예를 들어, 출력 채널에 대해 절반 끊기를 마친 후에도 입력 채널에 대한 상태 검사를 주기적으로 수행해줄 필요가 있다.
  - 이 경우, 입력 채널이 특정한 타임아웃 시간 내에 끊기지 않았다면 애플리케이션은 커넥션을 강제로 끊을 수 있다.

## 2026-02-18 Wed
### 웹 서버란?
- 웹 서버는 월드 와이드 웹의 일부로서, HTTP 요청을 처리하고 응답을 반환하는 역할을 수행한다.
  - 이 때, 웹 서버라는 용어는 물리적인 서버 자체와 웹 서버 소프트웨어 양쪽을 모두 지칭한다.
  - 반면, 웹 서버는 기능과 목적에 따라 다양한 규모로 구현될 수 있으나 모두 HTTP 요청을 받아 이를 처리한 후 클라이언트에게 응답한다는 공통점을 갖는다.
- 웹 서버는 HTTP나 그와 관련이 있는 TCP 처리를 구현하며, 주로 자신이 제공하는 리소스를 관리하기 위한 것과 웹 서버를 관리하기 위한 기능을 제공한다.
  - 이렇듯 **웹 서버는 HTTP 프로토콜을 구현하므로 TCP 커넥션 관리에 대한 책임을 운영체제와 나누어 갖는다**.
  - 예를 들어, 운영체제는 컴퓨터 시스템의 하드웨어를 관리하고 네트워크를 지원하며 파일 시스템과 프로세스 관리 기능을 제공한다.

## 2026-02-19 Thu
### 여러 형태의 웹 서버 구현체
- 웹 서버는 용도와 형태에 따라 다양하게 분류되지만, 크게 다목적 소프트웨어 웹 서버와 임베디드 웹 서버로 구분할 수 있다.
- 다목적 소프트웨어 웹 서버는 네트워크에 연결된 표준 컴퓨터에 설치하고 실행할 수 있는 것으로, 거의 모든 운영체제에서 동작 가능하다.
  - 예를 들어 아파치나 마이크로소프트 웹 서버 및 nginx 등을 예로 들 수 있다.
- 임베디드 웹 서버는 임의의 제품에 내장되는 형태의 경량 웹 서버를 가리키며, 소비자가 제품을 웹 브라우저로 관리할 수 있도록 지원한다.
  - 예를 들어, 프린터나 인터넷 공유기 등에 내장되어 최소한의 기능을 제공한다.

## 2026-02-20 Fri
### 웹 서버가 하는 일
- 상용 웹 서버는 상당히 복잡하지만, 공통적으로 다음과 같은 작업을 수행한다.
  1. 커넥션 맺기
  2. 요청 수신하기
  3. 요청 처리하기
  4. 리소스에 접근하기
  5. 응답 생성하기
  6. 응답 송신하기
  7. 트랜잭션을 로깅하기
- 이렇듯 웹 서버는 적절한 클라이언트로부터 요청을 수신하고, 처리한 후 적절한 응답을 반환하는 형태로 동작한다.

## 2026-02-21 Sat
### 클라이언트의 커넥션 수락하기
- 이미 맺어진 지속적인 커넥션이 있다면 클라이언트는 이를 재사용할 수 있으나, 그렇지 않은 경우 새로운 커넥션을 맺을 필요가 있다.
- **클라이언트가 웹 서버에 새로운 TCP 커넥션을 요청한 경우, 웹 서버는 커넥션을 맺고 이로부터 IP 주소를 추출하여 상대편 클라이언트를 확인**한다.
- 웹 서버는 커넥션을 원할 때에 거절하거나 닫을 수 있으므로, 구현에 따라서는 악의적인 것으로 알려진 커넥션을 즉시 닫도록 동작할 수도 있다.
  - 대부분의 웹 서버는 역방향 DNS를 사용하여 클라이언트의 IP 주소를 호스트 명으로 변환하도록 설정되어 있다.
  - 웹 서버는 이러한 클라이언트 호스트 명을 접근 제어와 로깅에 사용할 수 있으나, 이러한 lookup 작업은 웹 트랜잭션을 느려지게 하는 주요한 원인이기도 하다.
  - 때문에 대부분의 대용량 웹 서버는 호스트명 분석 기능을 명시적으로 비활성화하거나, 특정 컨텐츠에 대해서만 활성화하도록 설정한다.
- 웹 서버는 클라이언트의 사용자 이름을 얻기 위해 `ident` 프로토콜을 지원하는 경우가 있으며, 클라이언트가 이를 지원하는 경우 113 포트를 기반으로 동작한다.
  - 그러나 **`ident` 프로토콜은 HTTP 트랜잭션을 유의미하게 지연시키고, 안전하지 않기에 조직 내부가 아니라면 잘 사용되지 않는다**.

## 2026-02-22 Sun
### 요청 메시지 수신 단계
- 웹 서버는 커넥션에 도착한 데이터를 다음과 같은 순서로 읽어들여 요청 메시지 형태로 파싱한다.
  1. 요청줄은 공백으로 분리되고 CRLF로 끝나며, 메소드와 URI 및 버전 번호를 찾아 파싱한다.
  2. 헤더의 끝인 CRLF로 끝나는 빈 줄을 찾을 때까지 CRLF로 끝나는 모든 메시지 헤더를 찾아 파싱하여 헤더 목록을 구성한다.
  3. 요청 본문이 있다면 읽어들이되, 길이는 `Content-Length` 헤더에 따른다.
- 반면, 커넥션은 언제든지 무효화될 수 있기에 웹 서버는 이에 대비할 필요가 있다.
  - 예를 들어, **웹 서버는 파싱하여 이해 가능한 수준에 이르기까지 읽어들인 메시지의 일부를 메모리에 임시 저장**한다.

## 2026-02-23 Mon
### 웹 서버의 커넥션 입출력 처리 아키텍쳐
- 좋은 웹 서버는 수천 개의 커넥션을 동시에 열 수 있도록 지원하며, 각 커넥션은 전세계의 클라이언트 중 일부와 하나 이상 연결된다.
  - 웹 서버는 새로운 요청에 대비하고 있으며, 모든 커넥션의 처리 속도는 같지도 않다.
- 웹 서버는 각 커넥션을 처리하는 방법에 따라 서로 다른 아키텍쳐로 구성될 수 있으며, 이를 다음과 같이 구분할 수 있다.
  1. 단일 스레드: 한 번에 하나의 요청을 처리하며, 처리 도중 요청된 새로운 커넥션은 무시된다.
  2. 멀티프로세스 및 멀티스레드: 여러 요청을 동시에 처리할 수 있으나, 각 워커는 메모리를 소비하므로 최대 개수에 제한을 둔다.
  3. 다중 IO: 모든 커넥션은 감시되며, 커넥션의 상태가 변경될 때에만 적절한 처리가 적용된다.
  4. 다중 멀티스레드: 멀티스레딩과 다중 IO 방식을 결합한 것으로, 커넥션 상태 변경에 따른 처리는 각 스레드가 담당한다.

## 2026-02-24 Tue
### 요청 처리 단계
- 웹 서버는 자신에게 전달된 요청을 분석하여 동작하며, 예를 들어 요청으로부터 메소드와 리소스 및 헤더 등을 읽어들여 처리한다.
- 이 때, 여러 메소드 중 `POST` 등의 일부 메소드는 요청 메시지에 본문이 포함되었음을 기대한다.
  - 반면, `OPTIONS`와 같은 일부 메소드는 본문을 허용하지만 필수적으로 요구하지는 않는다.
  - 물론 `GET`과 같이 본문 자체를 금지하는 메소드 역시 존재한다.
- 이렇듯 웹 서버는 자신이 전달 받은 HTTP 메시지의 내용에 따라 서로 다른 처리 방식을 적용하게 된다.

## 2026-02-25 Wed
### 리소스 매핑 및 접근 단계
- 웹 서버는 일종의 리소스 서버이며, 예를 들어 HTML이나 JPEG 등 미리 생성된 컨텐츠를 제공한다.
  - 물론, 서버로부터 동작하는 동적인 리소스 생성 애플리케이션을 통해 생성된 컨텐츠 역시 제공될 수 있다.
- 웹 서버가 클라이언트가 요청한 컨텐츠를 제공하기 위해서는 요청 메시지의 URI에 대응하는 컨텐츠를 식별할 수 있어야 한다.
  - 예를 들어, 적절한 컨텐츠나 컨텐츠 생성기를 웹 서버로부터 찾아 식별할 수 있어야 한다.
- 웹 서버는 여러 종류의 리소스 매핑을 지원하며, 가장 단순한 형태로는 URI를 웹 서버의 파일 시스템에 대입하는 방식이 있다.
  - 이 경우, 웹 서버에 있는 임의의 폴더를 웹 컨텐츠를 위해 예약하여 `docroot`로 지정하게 된다.

## 2026-02-26 Thu
### Docroot란?
- 웹 서버의 파일 시스템에 위치한 임의의 폴더를 웹 컨텐츠를 위해 예약할 경우, 이를 문서 루트 또는 `docroot`라는 용어로 지칭할 수 있다.
- 아파치 웹 서버를 예로 들어, `httpd.conf`에 `DocumentRoot`를 명시하는 것으로 문서 루트를 지정할 수 있다.
  - 예를 들어 `/usr/local/httpd/files`가 문서 루트로 명시될 수 있다.
  - 이 경우, 사용자가 `/image/something.png`를 요청할 경우 문서 루트에서 `image` 폴더를 찾아 반환하게 된다.
- 일반적으로, 웹 서버는 `../`와 같은 상대 경로에 의해 문서 루트를 벗어난 파일 시스템이 노출되지 않도록 동작한다.

## 2026-02-27 Fri
### 가상 호스팅된 Docroot
- 가상 호스팅이 적용된 웹 서버는 각 사이트마다 분리된 문서 루트를 지정할 수 있으며, 이로 인해 하나의 웹 서버에서 여러 개의 웹 사이트를 호스팅할 수 있다.
- 가상 호스팅 웹 서버는 URI나 `Host` 헤더의 정보와 호스트 명을 활용하여 적절한 문서 루트를 결정한다.
  - 이를 통해 하나의 웹 서버 상에서 두 개의 사이트가 완전히 분리된 컨텐트를 제공하도록 호스팅될 수 있다.
- 대부분의 웹 서버는 가상 호스팅 기능을 간단히 지원하며, 아파치 웹 서버의 경우 `VirtualHost` 키워드를 활용한다.

## 2026-02-28 Sat
### 디렉토리에 대한 요청 처리
- 웹 서버는 요청 경로가 파일이 아닌 디렉토리를 가리키는 요청을 수신할 수 있으며, 이 경우 다음과 같은 행동 중 하나를 취할 수 있다.
  1. 에러 반환하기
  2. 디렉토리 내의 색인 파일 반환하기
  3. 디렉토리를 탐색한 후 이를 표현한 페이지를 반환하기
- 대부분의 웹 서버는 `index.html`과 같은 파일을 반환하며, 아파치 웹 서버의 경우 `DirectoryIndex` 설정으로 색인 파일 이름의 집합을 설정할 수 있다.
  - 해당 키워드는 색인 파일로 취급될 수 있는 파일 이름을 우선 순위에 따라 나열한다.
- 반면, 웹 서버에 디렉토리 색인 기능이 활성화되어 있으나 색인 파일이 없다면 해당 디렉토리를 탐색하여 파일 목록과 링크를 제공하는 HTML 파일을 반환한다.
  - 이 방식은 **편리하지만, 일반적으로는 보여질 수 없는 파일도 함께 반환한다는 단점을 수반**한다.
  - 때문에 아파치 웹 서버의 경우 `Options -Indexes` 옵션으로 디렉토리 색인 파일 자동 생성 기능을 비활성화할 수 있다.

## 2026-03-08 Sun
### 동적 컨텐츠 리소스 매핑하기
- 웹 서버는 요청에 맞는 리소스를 생성하는 프로그램에 URI를 매핑할 수 있으며, 이를 동적 컨텐츠 리소스 매핑이라는 용어로 지칭할 수 있다.
- 이 때, 애플리케이션 서버라고 불리우는 유형의 웹 서버는 웹 서버와 복잡한 서버 애플리케이션을 연결하는 역할을 수행한다.
  - 이러한 애플리케이션 서버는 적절한 동적 컨텐츠 생성 프로그램이 어느 위치에 있는지와 이를 어떻게 실행할 수 있는지 알려줄 수 있어야 한다.
- CGI와 같은 웹 초기 방식은 서버측 애플리케이션을 실행하기 위한 간단한 인터페이스를 제공한다.
  - 그러나 **최근의 애플리케이션 서버는 Java 서블릿과 같이 더 강력하고 효과적인 서버측 동적 컨텐츠 생성 기능을 지원**한다.

## 2026-03-09 Mon
### SSI란?
- SSI는 `Server-Side Includes`의 약자로, 서버가 임의의 리소스를 클라이언트에게 반환하기 전에 전처리를 적용하는 방식을 지칭한다.
  - 많은 웹 서버는 이러한 SSI 방식을 지원하며, 임의의 리소스가 SSI를 포함하도록 설정된다면 서버는 이에 대해 전처리를 적용한다.
- 서버는 SSI가 포함되도록 설정된 컨텐츠에 대한 요청을 다음과 같이 처리한다.
  1. 컨텐츠에 미리 정해진 변수나 임의의 패턴이 포함되었는지 검사한다.
  2. 포함된 경우, 실행 가능한 스크립트를 출력한 값을 각 변수나 패턴으로 치환하여 동적 컨텐츠를 생성한다.

## 2026-03-10 Tue
### 응답 생성을 위한 MIME 타입 결정 방식
- 서버가 리소스를 식별한 후라면, 서버는 요청 메소드에 맞는 동작을 수행한 후 응답 메시지를 생성하여 반환한다.
- 이 때, 응답이 본문을 포함하는 경우 응답 메시지에는 `Content-Type`과 `Content-Length` 등의 헤더가 포함된다.
- 이는 바꿔 말해 서버가 MIME 타입을 결정할 수 있어야 함을 의미하며, 웹 서버는 MIME 타입 결정을 위해 다음과 같은 방식 중 하나를 사용할 수 있다.
  1. `mime.types` 방식: 가장 흔한 방식으로, 파일의 확장자를 통해 MIME 타입을 결정한다.
  2. 매직 타이핑 방식: 확장자가 없는 파일의 경우 파일을 직접 검사하여 알려진 패턴을 명시하는 매직 파일로부터 적절한 패턴을 찾는 느린 방식을 의미한다.
  3. 유형 명시 방식: 임의의 디렉토리 내의 파일은 확장자와 무관하게 미리 정의된 MIME 타입으로 취급하도록 설정하는 것을 의미한다.
  4. 유형 협상 방식: 임의의 리소스가 여러 문서 형식에 속하는 경우, 클라이언트와 서버 간의 일련의 협상 과정을 거쳐 적절한 형식을 판별하는 것을 의미한다.

## 2026-03-11 Wed
### 웹 서버와 리다이렉션
- 웹 서버는 종종 성공 메시지 대신 리다이렉션 응답을 반환할 수 있으며, 브라우저는 리다이렉션 응답을 처리하기 위해 다른 위치로 리다이렉트할 수 있다.
- 이러한 리다이렉션 동작은 `3xx` 상태 코드와 `Location` 헤더의 조합으로 처리되며, 특히 다음과 같은 경우에 유용하다.
  1. 리소스가 영구히 옮겨짐: 리다이렉션 동작에 더해 클라이언트가 북마크를 갱신하도록 유도한다.
  2. 리소스가 임시로 옮겨짐: 일시적인 이동이므로 북마크 갱신을 유도하지 않고, 추후에는 원본 URL로 돌아올 것을 기대한다.
  3. URL 증강: 웹 서버가 의도적으로 임의의 정보를 포함시키기 위해 재작성된 URL로 리다이렉트 시키는 방식을 의미한다.
  4. 부하 균형: 과부하된 서버가 새로운 요청을 수신한 경우, 부하가 덜한 서버로 클라이언트를 유도할 수 있다.
  5. 친밀한 서버 활용: 임의의 웹 서버가 다른 친밀한 서버에 정보가 있음을 알고 있는 경우에 해당한다.
  6. 디렉토리 이름 정규화: 클라이언트가 올바르지 않은 URI를 요청했으나 웹 서버가 이를 교정할 수 있는 경우, 적절한 URI로 리다이렉트하도록 유도할 수 있다.

## 2026-03-12 Thu
### 응답 보내기 단계
- 웹 서버는 수많은 클라이언트와 연결된 다수의 커넥션을 가질 수 있으므로, 각 커넥션의 상태를 모니터링할 필요가 있다.
  - 예를 들어, 웹 서버와 연결된 커넥션 중 일부는 지속적이거나 비지속적일 수 있다.
- 지속적인 커넥션의 경우, 웹 서버는 `Content-Length` 헤더에 명시될 값이 올바르도록 주의해야 할 수 있다.
  - 또는 클라이언트의 응답이 언제 종료될지 알 수 없는 경우에도 웹 서버는 커넥션을 유지해야 한다.
- 비지속적인 커넥션의 경우, 웹 서버는 모든 메시지를 전송했을 때 자신의 커넥션을 닫도록 동작해야 한다.
- 반면, 일련의 HTTP 트랜잭션이 완료된 경우 웹 서버는 트랜잭션이 어떻게 수행되었는지에 대한 정보를 별도의 로그 파일에 기록할 수 있다.
  - 대부분의 웹 서버는 이러한 로깅과 관련된 여러 설정 양식을 제공하기도 한다.

## 2026-03-13 Fri
### 프록시란?
- 프록시는 클라이언트 관점에서 트랜잭션을 중개하므로, 실제 클라이언트는 프록시가 제공하는 서비스의 혜택을 누릴 수 있게 된다.
  - 프록시는 클라이언트 관점에서 웹 서버이며, 웹 서버의 관점에서는 클라이언트처럼 동작할 수 있어야 한다.
- 프록시는 형태에 따라 단일 클라이언트만을 위해 동작하는 개인 프록시와 여러 클라이언트가 공유하는 공유 프록시로 구분할 수 있다.
  - 대부분의 프록시는 공유 프록시이며, 중앙 집중화를 통해 비용효율성을 높이는데에 의의를 둔다.
  - 반면, 개인 프록시는 매우 한정적인 용도로 사용된다.

## 2026-03-14 Sat
### 프록시와 게이트웨이 비교하기
- 엄격히 분류했을 때, 프록시와 게이트웨이는 다음과 같은 차이점을 갖는다.
  1. 프록시: 동일한 프로토콜을 사용하는 둘 이상의 애플리케이션을 연결한다.
  2. 게이트웨이: 서로 다른 프로토콜을 사용하는 둘 이상의 애플리케이션을 연결한다.
- 때문에 게이트웨이는 클라이언트와 서버가 서로 다른 프로토콜로 대화하더라도 서로 간의 트랜잭션을 완료할 수 있도록 하는 일종의 프로토콜 변환기로 기능한다.
- 반면, **실질적으로 브라우저와 서버가 서로 다른 HTTP 버전을 구현하거나 프로토콜 변환을 적용하는 경우가 있기에 두 개념의 차이는 모호**하다.

## 2026-03-15 Sun
### 프록시의 용도
```
> 프록시는 실용적이고 유용한 것은 무엇이든지 할 수 있다.
```
- 프록시는 보안을 향상시키거나 성능 향상, 비용 절약 등 여러 혜택 제공할 수 있으며 이를 위해 트래픽을 감시하거나 수정할 수 있다.
- 예를 들어 유해 사이트를 필터링이나 캐싱 기능, 또는 접근 제어 등의 보안 기능을 제공할 수 있다.
  - 그 밖에도 리버스 프록시나 트랜스코딩 계층 제공 및 익명화 기능 등등 클라이언트와 서버 사이에서 어떠한 혜택을 주는 것은 모두 프록시로 취급될 수 있다.

## 2026-03-16 Mon
### 프록시의 위치
- 프록시는 용도에 따라 배치 위치가 변경될 수 있으며, 주로 배치되는 위치에 따라 다음과 같이 구분할 수 있다.
  1. 출구 프록시: 로컬 네트워크로부터 인터넷으로 나가기 전에 배치된다.
  2. 입구 프록시: ISP에 접근하기 직전의 위치에 배치되어 ISP에 의도에 따라 동작한다.
  3. 대리 프록시: 서버 측에 배치되며, 주로 리버스 프록시라는 용어로 지칭된다.
  4. 네트워크 교환 프록시: 네트워크 사이, 즉 라우터 사이에 배치되어 네트워크의 트래픽 감시 및 혼잡 완화 등의 기능을 제공한다.
- 이 때, 프록시는 의도에 따라 계층적인 구조를 두어 연쇄되는 형태를 가질 수 있다.
  - 예를 들어, 클라이언트와 웹 서버 사이에 여러 개의 프록시가 배치될 수 있다.
  - 또한, 각 프록시 연쇄는 항상 정적인 흐름을 갖는 것은 아니며 필요에 따라 동적인 연쇄 방식을 취할 수도 있다.

## 2026-03-17 Tue
### 프록시를 도입하는 여러 방법
- 일반적으로, 클라이언트는 웹 서버와 직접 대화하므로 프록시를 도입하기 위해서는 클라이언트로부터 발생한 트래픽이 프록시로 향하도록 설정할 필요가 있다.
- 이 때, 트래픽의 방향을 변경하기 위해서는 다음과 같은 방법 중 하나를 고려할 수 있다.
  1. 클라이언트를 수정하여 원 서버 대신 프록시 서버로 트래픽이 흐르도록 한다.
  2. 클라이언트 대신 인프라 구조를 수정하여 트래픽을 가로채도록 설정한다.
  3. DNS를 수정하여 모든 요청이 서버 대신 프록시로 흐르도록 설정한다.
  4. 웹 서버를 수정하여 리다이렉션 코드를 반환하고, 브라우저가 이에 따라 자연스레 리다이렉트하도록 설정한다.

## 2026-03-18 Wed
### 프록시 URI와 서버 URI의 차이
- 웹 서버와 프록시의 메시지 문법은 같지만, 클라이언트가 요청하는 대상의 종류에 따라 URI는 달라질 수 있다.
  - 예를 들어, 클라이언트가 웹 서버로 직접 요청하는 경우 요청줄의 URI는 부분 URI가 된다.
  - 반면, 프록시로 요청하는 경우 요청줄의 URI는 완전한 형태를 갖는다.
- 이는 초기 HTTP 인프라에 프록시 개념이 없었기 때문으로, 프록시가 대상이 되는 웹 서버를 특정짓기 위해서는 전체 URI가 필요하다.
  - **이상적으로는 모두 전체 URI를 사용하는 방식으로 전환하는 것이나, `HTTP/1.0` 제안시 이미 너무 많은 서버가 부분 URI를 사용**하고 있었다.

## 2026-03-19 Thu
### 가상 호스팅과 전체 URI
- 가상 호스팅을 제공하는 웹 서버는 동일한 물리 서버가 여러 웹 사이트를 제공하므로, 부분 URI를 사용하는 경우 상술한 문제가 동일하게 발생하게 된다.
  - 예를 들어, `/index.html`이라는 리소스가 요청된 경우 가상 호스팅된 웹 서버는 어떤 웹 사이트의 리소스인지 구분할 수 없다.
- 명시적인 프록시가 배치된 경우 전체 URI를 통해 이러한 문제를 해결하나, 가상 호스팅 서버는 이와 다른 해결책을 택하게 되었다.
  - 직접적으로 말해, **가상 호스팅된 웹 서버는 호스트와 포트 정보를 갖는 `Host` 헤더를 요구**한다.

## 2026-03-20 Fri
### 명시적이지 않은 프록시의 경우
- 상술한 프록시 문제의 해결법은 클라이언트가 프록시의 존재를 명확히 인지한 경우에 해당하는 반면, 클라이언트는 프록시의 존재를 인지하지 못할 수 있다.
  - 이 경우, 클라이언트는 자신이 웹 서버와 통신하고 있다고 생각하므로 여전히 부분 URI를 전송한다.
- 그러나 이 경우, 리버스 프록시와 인터셉트 프록시는 모두 원 서버의 존재를 알고 있기에 부분 URI 만으로도 정상 동작이 가능하다.
- 상술한 문제는 프록시가 어떤 대상 서버로 요청을 중개할지 알 수 없기 때문에 생긴 것으로, 프록시가 대상 서버를 항상 알고 있다면 문제가 발생하지 않는다.

## 2026-03-21 Sat
### 프록시가 URI 형태를 결정하는 방법
- 상술한 내용에 따라, 다목적 프록시 서버는 완전한 URI와 부분 URI를 모두 지원할 수 있어야 한다.
  - 이 때, 동작 과정에서 완전한 URI와 부분 URI 및 `Host` 헤더를 조합해야 한다.
- 프록시는 크게 다음과 같은 규칙에 따라 URI의 형태를 결정하게 된다.
  1. 완전한 URI가 요청에 포함된 경우, 프록시는 이를 그대로 중개한다.
  2. 부분 URI와 `Host` 헤더가 요청에 포함된 경우, `Host` 헤더를 통해 원 서버의 이름과 포트 번호 정보를 얻어 중개한다.
  3. 부분 URI가 주어졌으나 `Host` 헤더가 누락된 경우, 프록시는 자신의 설정을 확인하여 대상 서버의 정보를 얻어낼 수 있다.
  4. 부분 URI가 주어졌으나 `Host` 헤더가 누락되고 마땅한 설정도 없는 경우, 충분한 정보를 갖지 못했으므로 반드시 에러 메시지를 반환해야 한다.

## 2026-03-22 Sun
### 프록시와 URI 변경
- 프록시가 아무리 사소해보이는 URI 변경을 적용했더라도, 이는 클라이언트와 대상 서버 간의 상호운용성에 큰 문제를 일으킬 수 있다.
  - 이는 `HTTP` 요청에 누락된 `:80` 포트를 추가하는 동작이나 이스케이프를 적용하는 것과 같은 간단해보이는 동작까지도 포함한다.
  - 때문에 프록시는 가능한 한 관대하게 동작해야 하며, 상호운용성을 최대한 보장하는 방향으로 동작해야 한다.
- 예외적으로, **빈 경로를 `/`로 변경하는 정도의 URI 변경은 상호운용성에 큰 영향을 주지 않는다**.

## 2026-03-23 Mon
### 브라우저의 URI 확장에 프록시가 미치는 영향
- 브라우저는 프록시가 없는 경우, 사용자가 입력한 URI를 자동으로 확장한다.
  - 예를 들어, `something`을 입력했다면 이를 DNS에 질의한 후 적절한 IP 주소가 없다면 `www`와 `.com` 등이 누락된 것으로 보고 이를 확장한다.
- 그러나 명시적인 프록시가 설정된 경우, 브라우저는 이러한 확장 동작을 적용하지 않고 프록시에 그대로 전달하게 된다.
  - 때문에 몇몇 프록시는 브라우저의 URI 자동 확장과 유사한 기능을 제공하기도 한다.
  - 그러나 많은 프록시는 다수의 클라이언트에 의해 동작하므로, 각 클라이언트에 맞는 접두사나 접미사를 추측하는 것이 불가능하다는 한계를 갖는다.
- 반면, 브라우저가 DNS 동작에 성공했지만 해당 서버가 실제로는 동작하지 않는 경우 연결 경로 상 인터셉트 프록시가 존재한다면 문제가 발생할 수 있다.
  - 예를 들어, 브라우저는 인터셉트 프록시와 소통하므로 웹 서버가 살아있다고 믿지만 실제로는 웹 서버가 동작하지 않는 상황을 들 수 있다.
  - 이렇듯 **브라우저가 명시적인 프록시를 사용하도록 설정되어 있는 경우, 대상 서버에 대한 장애 허용성은 프록시의 설정에 따르는 점에 주의**해야 한다.
  - 때문에 프록시는 브라우저에서 제공하는 것과 동등한 수준의 장애 허용을 제공할 수 있도록 설정되어야 한다.

## 2026-03-24 Tue
### 프록시 연쇄에 따른 메시지의 추적을 위한 Via 헤더
- HTTP 인프라가 발전해감에 따라 프록시는 다양한 이유로 인해 점점 흔해졌으며, 이에 메시지의 흐름을 추적하는 것은 주요한 관심사로 떠오르게 되었다.
- 예를 들어 `Via` 헤더는 프록시나 게이트웨이 등의 중간 노드 정보를 나열하기 위해 사용되며, 중간 노드는 지속적으로 헤더 값의 끝에 추가된다.
  - 해당 헤더 필드는 메시지의 전달을 추적하고, 중간 노드들이 사용하는 프로토콜 정보를 확인하기 위해 사용될 수 있다.
  - 반면, 요청 메시지와 응답 메시지는 모두 프록시를 지나므로 모두 `Via` 헤더를 가지며 그 특성상 요청과 응답에 포함된 값은 언제나 반대가 된다.
- 응답 헤더에 포함되는 `Server`는 대상 서버에 사용되는 소프트웨어 정보를 명시하며, 이로 인해 프록시는 해당 헤더를 수정하지 않아야 한다.
  - 해당 헤더는 어디까지나 대상 서버의 정보를 명시하기 위함으로, 프록시는 대신 `Via` 헤더에 내용을 추가할 수 있다.
- 반면, 보안상 경유지 정보를 노출하지 않고자 하는 경우 그 경계에 포함된 호스트의 정보를 하나로 합쳐 `Via` 헤더에 명시할 수 있다.
  - 바꿔 말해 중간 노드들이 동일한 조직의 통제하에 있는 것이 아니라면 항목들을 합치지 않아야 한다.

## 2026-03-25 Wed
### 프록시와 인증
- 프록시는 접근 제어 장치로 동작할 수 있으며, HTTP는 클라이언트가 적절한 자격 증명을 제출하지 않는 한 프록시에 의해 요청이 차단될 수 있다.
- 예를 들어, 클라이언트가 제한된 컨텐츠를 프록시에 요청한 경우 다음과 같은 절차가 진행된다.
  1. 프록시는 접근을 위한 자격 증명을 요청하는 `407 Proxy Authorization Required`로 응답한다.
  2. 이 때, 프록시는 응답에 `Proxy-Authenticate` 헤더에 자격 증명에 대한 제출 방법을 명시한다.
  3. 클라이언트는 상술한 헤더에 맞는 자격 증명을 스스로 얻어낸 후, `Proxy-Authorization` 헤더에 이를 명시하여 재요청한다.
  4. 프록시는 해당 자격 증명이 유효한 경우에만 프록시 연쇄를 통과시키며, 그렇지 않은 경우 다시 `407`로 응답한다.
- 반면, **프록시 인증은 인증에 참여하는 프록시가 연쇄상 여럿 존재할 경우에는 잘 동작하지 않는다는 한계**를 갖는다.

## 2026-03-26 Thu
### 프록시와 상호운용성
- `HTTP/1.1`은 메소드 확장이나 사용자 정의 헤더를 허용하므로, 프록시는 자신이 이해하지 못하는 메소드와 헤더를 그대로 유지하도록 동작해야 한다.
- 또한, 클라이언트는 `OPTIONS` 메소드를 전달하는 것으로 대상 서버가 어떤 메소드를 제공하는지에 대한 정보를 얻어낼 수 있다.
- 예를 들어 `HTTP/1.1`의 경우, 서버에 어떤 메소드가 지원되는지 서술하기 위해 `Allow` 헤더에 서버에서 지원하는 메소드의 목록을 명시할 수 있다.
  - 반면, 클라이언트에 의한 요청 메시지에 `Allow` 헤더가 포함된 경우 해당 리소스에 대해 클라이언트가 희망하는 메소드를 의미하게 된다.
  - 이에 대상 서버는 해당 리소스에서 실제로 지원하는 메소드만을 `Allow` 헤더에 명시하여 응답하며, 클라이언트의 요청 메소드를 모두 지원해야 할 의무는 없다.
  - **프록시의 경우, `Allow` 헤더에 명시된 모든 메소드를 이해할 수 없다고 하더라도 해당 헤더의 내용을 수정하지 않아야 한다**.

## 2026-03-27 Fri
### 웹 캐시란?
- 웹 캐시는 자주 사용되는 문서의 사본을 자동으로 보관하는 HTTP 장치를 의미한다.
  - 새로운 웹 요청이 캐시에 도달했을 때, 캐시에 적절한 사본이 존재한다면 원 서버에 요청하지 않을 수 있다.
- 캐시는 IO 작업의 횟수에 직접적인 영향을 줄 수 있으며, 적절히 설정된 경우 다음과 같은 이점을 제공한다.
  1. 불필요한 데이터 전송 방지
  2. 네트워크 병목 감소
  3. 원 서버 부담 감소
  4. 리소스 요청시 응답 지연 감소

## 2026-03-28 Sat
### 캐시 히트와 캐시 미스 및 재검사
- 캐시는 유용하지만, 세상 모든 문서의 사본을 저장할 수는 없으므로 부득이하게 필요한 문서만을 캐시하게 된다.
- 이 때, 임의의 요청에 대응되는 사본이 존재하는 경우를 캐시 히트라고 지칭할 수 있다.
  - 반면, 사본이 존재하지 않아 원 서버로 요청하는 경우는 캐시 미스가 된다.
- 그러나 원 서버의 리소스가 변경될 수 있으므로 캐시는 사본이 최신인지 서버를 통해 점검할 필요가 있으며, 이를 재검사라는 용어로 지칭한다.
  - 재검사 요청의 효율성을 높이기 위해, HTTP는 서버로부터 전체 객체를 가져오지 않고 재검사할 수 있도록 하는 특별한 요청을 정의한다.
  - 캐시는 임의의 사본에 대한 재검사가 필요한 경우 원 서버에 작은 요청을 전송하며, 서버는 변경되지 않은 경우에 `304`로 응답할 수 있다.
- HTTP는 캐시 재검사를 위해 `If-Modified-Since` 헤더를 제공하며, 서버는 해당 시간 이후에 리소스가 변경된 경우에만 사본을 전송한다.
  - 서버의 입장에서, 변경되지 않은 경우 `304`로 응답한다.
  - 변경된 경우, 서버는 `200`을 통해 리소스 전체를 반환한다.
  - 반면, **서버에서 해당 리소스가 삭제된 경우 `404`를 반환하게 되므로 캐시는 사본을 삭제**할 수 있다.
- 반면, HTTP는 클라이언트에게 캐시가 적중했는지 알려줄 수 있는 별도의 방법을 제공하지는 않는다.
  - 클라이언트는 대신 `Date` 헤더나 `Age` 헤더를 통해 응답의 캐싱 여부를 추측할 수 있다.

## 2026-03-29 Sun
### 히트율과 바이트 적중률
- 캐시가 요청을 처리하는 비율을 히트율(Hit ration)이라고 표현하며, 일반적으로 40%의 히트율을 보장할 경우 좋은 캐시로 취급할 수 있다.
  - 이를 개선할 경우, 외부로 나가는 HTTP 트랜잭션이 줄어들어 전체 지연이 감소한다.
- 반면, 바이트 적중율은 캐시를 통해 트래픽이 절감된 양을 측정하는 데에 초점을 맞춘다.
  - 이를 개선할 경우, 외부로 나가는 바이트의 양이 줄어들어 대역폭을 크게 절약할 수 있다.

## 2026-03-30 Mon
### 캐시 토폴로지
- 캐시는 공유 대상의 규모에 따라 크게 다음과 같이 구분할 수 있다.
  1. 개인용 캐시: 브라우저 캐시와 같이 사용자 한 명을 위한 작고 저렴한 캐시를 의미한다.
  2. 공용 프록시 캐시: 캐싱 기능을 제공하는 일종의 프록시이며, 여러 사용자가 접근할 수 있다.
- 이 때, 작고 저렴한 캐시에서 캐시 미스가 발생했을 때 프록시 연쇄에 따라 더 성능이 좋은 부모 캐시가 이를 처리하도록 구현할 수 있다.
  - 이는 일종의 캐시 계층으로, 둘 혹은 세 개의 캐시 프록시 연쇄를 두는 합리적인 방식에 해당한다.
- 반면, 캐시 계층보다 더 복잡한 형태의 캐시망을 구현할 수도 있으며 이 경우 캐싱 과정을 동적으로 결정하는 특징을 갖는다.

## 2026-03-31 Tue
### 캐시 처리 단계
- 웹 캐시의 기본적인 동작은 단순하며, 예를 들어 임의의 `GET` 메시지에 대한 처리 단계는 다음과 같이 표현할 수 있다.
  1. 캐시는 네트워크로부터 도착한 요청 메시지를 읽어들인다.
  2. 메시지를 파싱하여 URL과 헤더 정보를 추출한다.
  3. 이에 대한 로컬 복사본이 있는지 검사하고, 없는 경우 사본을 받아와 로컬에 저장한다.
  4. 캐시된 사본이 충분히 신선한지 검증한 후, 신선하지 않은 경우 서버에 변경 사항을 질의한다.
  5. 캐시는 새로운 헤더와 캐시된 본문을 기반으로 응답 메시지를 생성한 후, 이를 클라이언트에게 반환한다.

## 2026-04-01 Wed
### 캐시와 신선도
- 캐시된 사본은 항상 서버의 문서와 일치함을 보장할 수 없으며, 일반적으로 문서들은 시간에 따라 변동된다.
  - 때문에 캐시는 오래된 데이터를 제공하지 않아야 하며, 캐시된 데이터가 서버의 데이터와 일치하도록 관리되어야 한다.
- HTTP는 어떤 캐시가 어떤 문서의 사본을 갖는지 서버가 알 필요 없이 신선도를 유지하도록 하기 위한 간단한 메커니즘인 문서 만료와 서버 재검사를 제공한다.
  - 예를 들어, **`Cache-Control`과 `Expires`와 같은 헤더를 통해 서버가 문서에 유효기간을 명시할 수 있도록 지원**한다.
  - `Expires`는 해당 문서가 만료되는 절대적인 시점을 명시하는 반면, `Cache-Control`은 `max-age` 옵션을 통해 만료되기까지의 시간을 명시한다.
  - 추가적으로, `Expires`는 `HTTP/1.0`의 개념인 반면 `Cache-Control`과 `max-age`는 `HTTP/1.1`의 개념에 해당한다.
- **클라이언트가 캐시된 리소스의 제공을 거부하지 않는 이상 캐시는 만료되지 않은 사본을 반환할 수 있으며, 만료시 서버에 변경점을 질의**해야 한다.

## 2026-04-02 Thu
### 서버 재검사란?
- **캐시된 문서의 만료는 해당 문서가 서버의 리소스와 항상 다른 것은 아니며, 다만 재검사할 시간이 되었음을 의미**한다.
  - 이렇듯 캐시가 대상 서버에게 문서의 변경 여부를 질의하는 일련의 과정을 서버 재검사라는 용어로 지칭한다.
- **서버 재검사시, 캐시는 원본이 변경되었다면 새로운 사본으로 업데이트하는 반면 변경되지 않은 경우 새로운 만료일로 갱신**한다.
  - 이러한 방식은 문서 만료시에만 검사를 유발하므로 신선하지 않은 컨텐츠를 사용자에게 제공하지 않으면서도 서버의 부담을 줄이는 좋은 방법에 해당한다.
- HTTP 프로토콜은 캐시가 신선하거나, 재검사로 인해 신선하다고 확신 가능한 사본을 반환하거나 에러 메시지를 반환하는 행동 중 하나를 수행할 것을 기대한다.

## 2026-04-03 Fri
### 조건부 메소드란?
- HTTP의 조건부 메소드는 재검사 과정의 효율을 높여주며, 예를 들어 HTTP는 서버에게 조건부 `GET` 요청을 전송할 수 있도록 허용한다.
  - 이러한 **조건부 메소드는 요청에 `If-` 접두사를 갖는 조건부 헤더를 추가하는 것으로 사용이 가능**하다.
  - 조건부 `GET`을 예로 들어, 서버는 클라이언트가 요청한 조건에 부합하는 경우에만 응답 본문에 리소스 정보를 포함시킨다.
  - 이렇듯 **조건부 메소드는 신선도 검사와 객체 반환을 하나의 요청으로 결합시키는 데에 의의**를 둔다.
- HTTP는 다섯 개의 조건부 요청 헤더를 정의하며, 그 중 가장 유용한 것 두가지는 다음과 같다.
  1. `If-Modified-Since`: 요청된 문서가 명시된 날짜 이후로 수정된 경우에만 요청을 처리한다.
  2. `If-None-Match`: `ETag`와 같이 문서에 대한 일련 변호가 다른 경우에만 요청을 처리한다.

## 2026-04-04 Sat
### If-Modified-Since 헤더를 활용한 날짜 기반 재검사
- **재검사 과정에서 가장 자주 사용되는 헤더이며, 해당 헤더가 포함된 요청에 대해 서버는 문서가 명시된 날짜 이후로 변경된 경우에만 본문을 포함**시킨다.
- 예를 들어, 해당 헤더에 명시된 날짜와 원본 문서의 수정 일자를 비교했을 때 서버는 다음과 같이 동작한다.
  1. 문서가 명시된 날짜 이후에 변경된 경우: 조건이 참이므로, `GET` 요청은 평범하게 처리되어 새로운 만료 날짜가 포함된 헤더와 새 문서가 반환된다.
  2. 문서가 명시된 날짜 이후에 변경되지 않은 경우: 조건이 거짓이므로, 서버는 `304 Not Modified` 응답을 반환하되 본문은 포함하지 않는다.
- 이 때, **서버가 `304`로 응답하는 경우 응답 헤더에는 새로운 만료 날짜 등 갱신이 필요한 헤더들이 포함**되어야 한다.
  - 예를 들어, `Content-Type`과 같은 헤더는 잘 변경되지 않으므로 명시할 필요가 없다.
- 해당 헤더는 서버가 반환하는 `Last-Modified` 헤더와 함께 동작하며 대상 서버는 제공되는 문서에 대해 마지막 변경 일시를 해당 헤더에 명시한다.
  - 캐시는 캐시된 문서를 재검사하기 위한 마지막 수정 일시로 서버가 반환한 `Last-Modified` 헤더의 값을 활용한다.
- 반면, 일부 웹 서버는 `If-Modified-Since` 헤더와 관련된 동작을 실제 날짜 비교가 아닌 문자열 비교로 처리하는 경우가 있다.
  - 이 경우 정확히 이 날짜 이후로 변경되었는지 질의하는 것이 아닌, 수정 일시를 일종의 일련 번호로 취급하여 일련 번호의 변경 여부만을 검증한다.

## 2026-04-12 Sun
### If-None-Match 헤더를 활용한 엔티티 태그 기반 재검사
- 다음과 같은 유형의 문서 변경은 날짜 기반의 재검사를 활용하기엔 무리가 있을 수 있다.
  1. 일정 시간마다 문서가 다시 쓰여지나, 실제 내용에는 아무런 변화가 없는 경우.
  2. 주석의 변경 등으로 인해 전세계의 캐시 변경을 유발하기에는 사소한 변경 사항이 가해진 경우
  3. 서버가 문서의 변경 일시를 정확하게 파악할 수 없는 경우
  4. 1초보다 적은 간격으로 갱신되는 문서를 제공하는 서버의 경우
- 반면, 문서가 변경되었을 때 해당 문서의 관리자는 문서의 `ETag`를 새로운 버전으로 표현할 수 있는 경우가 있다.
  - 이렇듯 **`ETag`가 변경되는 경우에 캐시는 새로운 문서의 사본을 검증하기 위해 해당 헤더를 활용하는 조건부 요청을 전송**할 수 있다.
- `ETag` 기반의 검증 방식은 `If-Modified-Since`의 처리 방식과 유사하며, 단지 변경을 감지하기 위한 대상으로 날짜가 아닌 `ETag`를 사용한다.
  - 반면, 캐시가 문서에 대한 여러 사본을 갖는 경우 `If-None-Match` 헤더에 쉼표로 구분된 여러 개의 `ETag`를 명시할 수 있다.

## 2026-04-13 Mon
### 강한 검사기와 약한 검사기
- 상술한 바에 따라, 캐시는 캐시된 버전이 서버가 갖고 있는 것과 비교했을 때 최신인지 검증하기 위해 날짜나 `ETag`를 활용할 수 있다.
  - 이 경우, **날짜와 `ETag`는 모두 캐시 검사기라는 용어로 지칭**된다.
- 그러나 서버는 캐시된 사본을 무효화시킬만큼 문서가 크게 변경되지 않은 경우, '이 정도면 수정 전과 동일한 문서다'라고 주장하고 싶을 수 있다.
- `HTTP/1.1`은 이를 위해 컨텐츠가 약간 변경되었음을 서버가 주장할 수 있도록 `약한 검사기` 기능을 지원하며, `W/` 접두사로 구분된다.
  - 예를 들어, `ETag: W/"v2.6"`과 같이 명시하는 것으로 약한 검사기를 구분할 수 있다.
- **강한 검사기는 대응되는 문서가 변경될 때마다 반드시 변경되는 반면, 약한 검사기는 문서에 유의미한 변경이 있을 때에만 변경**된다.

## 2026-04-14 Tue
### 엔티티 태그 또는 날짜 기반 재검사의 적용 기준
- **`HTTP/1.1` 클라이언트는 서버가 `ETag` 헤더를 반환했다면 반드시 `ETag` 검사기를 사용**해야 한다.
  - 반면, 서버의 응답에 `Last-Modified` 헤더만이 포함되었다면 날짜 기반 검사기를 사용할 수 있다.
- 그러나 **클라이언트는 서버가 두 헤더를 모두 사용 가능하도록 응답을 반환한 경우에는 두 재검사 정책을 모두 사용할 필요**가 있다.
- `HTTP/1.1` 서버는 가능하다면 `ETag` 검사기를 반환해야 하며, 이점이 있다면 약한 `ETag`를 반환할 것을 고려해볼 수도 있다.
  - 또한, 가능하다면 `Last-Modified` 값을 함께 반환하는 것을 고려해야 한다.
- 반면, **`HTTP/1.1` 기반 서버 또는 캐시가 `If-Modified-Since`와 `If-None-Match`를 동시에 요청받은 경우 두 조건을 모두 검증**해야 한다.
  - 두 조건에 모두 부합하는 경우에만 `304`를 반환해야 하며, 그렇지 못한 경우라면 `304`를 반환하지 않아야 한다.

## 2026-04-15 Wed
### 캐시 제어하기
- **HTTP는 문서가 만료되기까지 얼마나 오랜 기간 캐시될 것인지 서버가 주도적으로 설정할 수 있도록 하는 여러 방법을 지원**한다.
- 이 때, 캐시 제어에는 주로 `Cache-Control` 헤더가 사용되며 그 외의 방법을 포함하여 우선 순위대로 나열하면 다음과 같다.
  1. `Cache-Control` 헤더에 `no-store`를 명시하기
  2. `Cache-Control` 헤더에 `no-cache`를 명시하기
  3. `Cache-Control` 헤더에 `must-revalidate`를 명시하기
  4. `Cache-Control` 헤더에 `max-age`를 명시하기
  5. `Expires` 헤더에 날짜 정보를 명시하기
  6. 아무 정보를 주지 않고, 캐시가 스스로 휴리스틱을 활용할 수 있도록 하기

## 2026-04-16 Thu
### no-store와 no-cache 방식
- 두 방식은 모두 캐시가 검증되지 않은 캐시된 객체로 응답하는 것을 방지하며, 크게 다음과 같은 차이를 갖는다.
  1. `no-store`: 캐시가 응답의 사본을 생성하는 것을 금지하며, 캐시는 클라이언트에게 응답을 전달한 후 객체를 삭제한다.
  2. `no-cache`: 로컬 캐시 저장소에 객체를 저장할 수는 있으나, 서버와 재검사를 거치지 않고서는 클라이언트에 객체를 제공하지 않는다.
- 반면, `HTTP/1.0+`와의 하위호환성을 위해 `Pragma: no-cache` 헤더를 사용할 수 있다.
  - `HTTP/1.1` 기반 애플리케이션은 `HTTP/1.0`을 사용하는 애플리케이션에 대응해야하는 경우 외에는 `Cache-Control: no-cache`를 사용해야 한다.

## 2026-04-17 Fri
### must-revalidate 방식
- 캐시는 성능 개선을 위해 이미 만료된 객체를 제공하도록 설정될 수도 있으므로, 캐시가 만료일 정도를 엄격히 따르길 원하는 경우 해당 방식을 명시할 수 있다.
  - 예를 들어, 서버는 `Cache-Control: must-revalidate`와 같이 명시된 헤더를 반환한다.
- 해당 응답 헤더는 캐시가 해당 객체의 신선하지 않은 사본을 서버와의 재검사 없이는 사용하지 말 것을 요청한다.
  - 반면, 캐시는 대상 서버와의 재검사 과정에서 대상 서버가 응답하지 않는 경우 `504` 응답을 반환해야 한다.

## 2026-04-18 Sat
### max-age 방식
- 신선하다고 간주된 문서가 서버로부터 반환된 이후로 흐른 시간을 초 단위로 나타낸 값을 명시하며, `s-maxage` 헤더와는 다음과 같이 구분된다.
  1. `max-age`: 문서가 서버로부터 온 후에 흐른 시간을 의미한다.
  2. `s-maxage`: 기본적으로 `max-age`처럼 행동하지만, 공유된 공용 캐시에만 사용된다.
- 반면, **서버는 `Cache-Control: max-age=0`으로 명시하는 것으로 캐시가 매 접근마다 문서를 캐시하지 않거나 갱신하도록 요청**할 수 있다.

## 2026-04-19 Sun
### Expires 헤더 방식
```
> 해당 헤더는 Deprecated로 권장되는 헤더임에 주의해야 한다.
```
- 초 단위의 시간을 명시하는 `max-age` 방식과 달리, `Expires` 헤더에는 실제 만료 날짜를 명시한다.
- 그러나 **HTTP 설계자들은 많은 서버가 정확하지 않은 시간 정보를 가지므로 절대 시각을 명시하는 방식보다 초 단위의 시간을 사용하는 것이 낫다고 판단**했다.
- 반면, 몇몇 서버는 문서가 항상 만료되도록 `Expires: 0` 응답 헤더를 명시하기도 하는 점에 주의해야 한다.
  - 그러나 이는 엄밀히 보았을 때 문법 위반이며, 몇몇 소프트웨어와 충돌하여 문제를 일으킬 수 있는 방식임에 주의해야 한다.
  - 때문에 이러한 값들을 가급적이면 받아들일 수 있도록 처리하는 반면, 자신이 개발한 서버가 이러한 헤더 쌍을 생성하지는 않도록 구현할 필요가 있다.

## 2026-04-20 Mon
### 휴리스틱 만료 방식
- **서버로부터의 응답에 상술한 어떠한 헤더도 포함하지 않는다면 캐시는 경험적인, 즉 휴리스틱 방식으로 문서의 최대 나이를 계산**할 수 있다.
  - 이를 위해 어떠한 알고리즘도 사용될 수 있으나, 최대 나이 값이 24시간보다 크다면 이를 경고하는 헤더가 응답에 추가될 필요가 있다.
- 대표적인 휴리스틱 만료 알고리즘에는 `LM 인자 알고리즘`이 있으며, 문서가 최근 변경 일시를 포함하는 경우에 적용이 가능하다.
  - `LM 인자 알고리즘은` 변경 일시를 토대로 문서가 얼마나 자주 변경될지 추정한다.
  - 예를 들어, 최근 변경 일시가 상당히 예전이라면 잘 변경되지 않는 문서로 추정하는 반면 최근이라면 자주 변경될 것으로 추정하여 적절한 캐싱 기간을 선정한다.
- 일반적으로 휴리스틱 신선도 유지 기간에 상한을 설정하여 너무 오래 캐싱되지 않도록 적절히 처리하는 것이 권장된다.
- 반면, 서버가 최근 변경일조차 반환하지 않은 경우 캐시는 아무런 정보가 없으므로 1시간이나 하루 등 기본 신선도 유지 기간을 설정하는 방식으로 동작한다.
  - 더 보수적인 캐시의 경우, 이러한 휴리스틱 문서들에 대해 신선도 수명을 0으로 설정하여 신선도 여부를 매번 검사하도록 강제한다.
- 예상보다 많은 대상 서버들이 상술한 어떠한 방식도 응답에 포함하지 않을 수 있기에 휴리스틱 신선도 계산은 꽤 자주 사용하게 될 수 있다.

## 2026-04-21 Tue
### 클라이언트 측의 신선도 제약
- 브라우저는 자기 자신이나 캐시의 신선하지 않은 컨텐츠를 강제로 갱신하기 위한 리로드 버튼을 포함하며, 이는 내부적으로 `Cache-Control` 헤더를 활용한다.
  - 즉, 클라이언트로부터의 요청에도 `Cache-Control` 헤더가 포함되어 강제 재검사 및 서버 객체 조회를 유발할 수 있다.
  - 이렇듯 클라이언트는 `Cache-Control` 헤더를 활용하여 만료 제약을 더 엄격하게, 또는 더 느슨하게 처리할 수 있다.
- 이 때, 클라이언트가 `Cache-Control` 헤더에 명시할 수 있는 방식은 크게 다음과 같다.
  1. `max-stale`: 캐시가 신선하지 않은 문서라도 제공할 수 있도록 요청하는 것으로 느슨한 캐싱 규칙을 적용한다.
  2. `min-fresh`: 지금으로부터 세어 명시된 초만큼 지난 시점에도 신선한 문서만을 제공하도록 요청하는 것으로 엄격한 캐싱 규칙을 적용한다.
  3. `max-age`: 명시된 초보다 오래 캐시된 문서를 반환하지 못하도록 요청하는 것으로 엄격한 캐싱 규칙을 적용한다.
  4. `no-cache`: 캐시가 임의의 객체에 대해 재검사하지 않는 이상은 받아들이지 않는다.
  5. `no-store`: 문서에 민감한 정보가 포함될 수 있으므로 캐시가 저장소로부터 문서를 최대한 빨리 삭제하도록 요청한다.
  6. `only-if-cached`: 캐시에 저장된 사본만을 요청한다.

## 2026-04-22 Wed
### 캐시 제어와 관련된 주의 사항
```
> 문서 만료는 완벽한 시스템이 아니다!
```
- 일부 퍼블리셔는 실수로 유효기간을 너무 먼 미래로 설정하는 경우가 있으므로, 클라이언트는 문서의 만료 기간을 절대적으로 신용하지 않아야 한다.
  - 이 경우, 너무 오랜 기간동안 해당 문서의 변경 사항이 반영되지 않을 수 있다.
- 또한, 여전히 많은 퍼블리셔는 문서에 적절한 유효기간을 사용조차 하지 않는다.
  - 이 경우, 캐시는 문서의 신선도 유지 기간을 파악하기 어려울 수 있다.